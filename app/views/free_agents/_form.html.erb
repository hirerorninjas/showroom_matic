<%= form_for(@free_agent) do |f| %>
  <% if @free_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@free_agent.errors.count, "error") %> prohibited this free_agent from being saved:</h2>

      <ul>
      <% @free_agent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Headline %>
    <%= f.text_field :Headline %>
  </div>
  <div class="field">
    <%= f.label :Details %>
    <%= f.text_area :Details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
