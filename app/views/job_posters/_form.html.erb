<%= form_for(@job_poster) do |f| %>
  <% if @job_poster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_poster.errors.count, "error") %> prohibited this job_poster from being saved:</h2>

      <ul>
      <% @job_poster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Headline %><br>
    <%= f.text_field :Headline %>
  </div>
  <div class="field">
    <%= f.label :Details %><br>
    <%= f.text_area :Details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
